# PWA / Web Push 快速參考

完整部署方式請參考 [`docs/web-push.md`](docs/web-push.md)。

Backend 與 Crawler Worker 必須設定相同的：

```dotenv
VAPID_PUBLIC_KEY=...
VAPID_PRIVATE_KEY=...
VAPID_SUBJECT=mailto:admin@example.com
```
